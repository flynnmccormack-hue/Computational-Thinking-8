# Section 1 - Helper functions (DON'T CHANGE!!)
import turtle, math, time, random
def set_background(image_filename):
	screen = turtle.Screen()
	try:
		screen.bgpic(f"/workspaces/Computational-Thinking-8/Backgrounds/{image_filename}.png")
	except:
		screen.bgpic(f"/workspaces/Computational-Thinking-8/Backgrounds/{image_filename}.gif")

def set_image(sprite, image_filename):
    image_file = f"./Images/{image_filename}.gif"
    screen = turtle.Screen()
    screen.register_shape(image_file)
    sprite.shape(image_file)

def create_sprite(image_filename, x=0, y=0):
    sprite = turtle.Turtle()
    set_image(sprite, image_filename)
    sprite.penup()
    sprite.goto(x,y)
    window.update()
    return sprite

def get_distance(s1, s2):
	dx = s1.xcor() - s2.xcor()
	dy = s1.ycor() - s2.ycor()
	return math.sqrt(dx*dx + dy*dy)

def draw_rectangle( color="black",x=0,y=0, width=100, height=100,):
	sprite = turtle.Turtle()
	sprite.speed(0)
	sprite.pencolor(color)
	sprite.color(color)
	sprite.penup()
	sprite.goto(x - (width*0.5), y + (height*0.5))
	sprite.pendown()
	sprite.begin_fill()
	for i in range(2):
		sprite.forward(width)
		sprite.right(90)
		sprite.forward(height)
		sprite.right(90)
	sprite.end_fill()
	sprite.hideturtle()


window = turtle.Screen()
window.tracer(0)


# Section 2: Setup
# TODO - create your player character
sprite1 = create_sprite("character1", 200, 100)
sprite2 = create_sprite("basketball", -200,100)
# TODO - set your background
set_background("hauntedhouse")
# TODO - set the starting value for your variable
tagger = "basketball"
sprite1 (points=0)
sprite2 (points=0)
# Section 3: Controls
def move_up1():
    sprite1.setheading(90)
    sprite1.forward(10)
	
def move_up2():
    sprite2.setheading(90)
    sprite2.forward(10)
	
def move_down1():
    sprite1.setheading(270)
    sprite1.forward(10)
	
def move_down2():
    sprite2.setheading(270)
    sprite2.forward(10)
	
def move_left1():
    sprite1.setheading(180)
    sprite1.forward(10)

def move_left2():
    sprite2.setheading(180)
    sprite2.forward(10)

def move_right1():    
    sprite1.setheading(0)
    sprite1.forward(10)

def move_right2():    
    sprite2.setheading(0)
    sprite2.forward(10)

def reset():
      sprite1.goto(200,100)
      sprite2.goto(-200,100)
  
window.onkeypress(move_up1, "w")
window.onkeypress(move_down1, "s")

window.onkeypress(move_left1, "a")
window.onkeypress(move_right1, "d")

window.onkeypress(move_up2, "Up")
window.onkeypress(move_down2, "Down")

window.onkeypress(move_left2, "Left")
window.onkeypress(move_right2, "Right")

window.onkeypress (reset, "e")

# Section 4: Game Loop
window.listen()
timer = 0
while True:
	time.sleep(0.1)
	timer += 1  
    if timer==600:
        break
    if num_tags==3:
        break
    # if get_distance(sprite1,sprite2)<30: 
    #   tagger (points+1)
 	# TODO - code for automatic actions






window.update()

	# if :
	# 	break
	

print("Game Over")